<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Operator.Views.PhotoDetailsView">
    <ContentPage.Content>
        <StackLayout>
            <Image Source="{Binding Photo.ImageSource}"/>
            <Label Text="{Binding Photo.Name}"/>
            <Label Text="{Binding Photo.Date}"/>
            <Label Text="{Binding Photo.Size}"/>
            <Button Text="D" Command="{Binding DownloadCommand}" HeightRequest="50"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>